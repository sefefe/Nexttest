name: pipeline

on:
  push:
    branchs: [main]

jobs:
  starter:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: check out
        uses: actions/checkout@v3
      - name: run lint
        run: npm run lint
